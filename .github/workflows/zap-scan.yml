name: OWASP-ZAP-Scan

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  zapscan:  # Ubah nama job -> tanpa underscore
    runs-on: ubuntu-latest
    name: Run-OWASP-ZAP-Baseline-Scan
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run OWASP ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.9.0
        with:
          target: "https://crud-manajemen-tugas-fullstack.vercel.app/"
          artifact_name: 'zap-scan-report' 
          cmd_options: "-J report_json.json -w report_md.md -r report_html.html"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: zap-scan-report   # ubah nama artifact -> huruf kecil + tanda hubung
          path: |
            report_html.html
            report_md.md
            report_json.json
